"""routing_engine — pcb.ai mature routing engine package.

Engine Step 0 (this commit): the T1-T9 ground-truth validation suite ONLY.
NO router/engine algorithm code lives here yet — only the fixture FORMAT, the
9 ground-truth fixtures, and the solver-agnostic test-runner harness.

See README.md for the schema + per-case ground-truth table, and
docs/ROUTING_ENGINE_DESIGN_2026-05-28.md §2/§3 for the design spec these
fixtures implement.
"""
